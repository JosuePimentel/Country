import { Component } from "@angular/core";
import { RouterLink } from "@angular/router";

@Component({
    selector: "app-back",
    standalone: true,
    imports: [RouterLink],
    templateUrl: "back.components.html"
})

export class BackComponent { }