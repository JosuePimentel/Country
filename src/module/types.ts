export type Filter = {
    nameFilter: string,
    countryFilter: string
}

export type CountryData = {
    name: string,
    region: string,
    population: number,
    capital: string,
    flag: string
}